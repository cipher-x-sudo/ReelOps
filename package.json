{
  "name": "reelops",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "renderer"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend && npm run build --workspace renderer",
    "dev:frontend": "npm run dev --workspace frontend",
    "render": "npm run render --workspace renderer"
  }
}

